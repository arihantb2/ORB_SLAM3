/**
 * Redirect header — ORBextractor has been replaced by ORBFeatureExtractor.
 * Kept only to avoid breaking src/ORBextractor.cc (which is not compiled).
 */
#pragma once

#include <feature_extractor/GridBasedORBFeatureExtractor.h>
