[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "orb-slam3-runners"
version = "0.1.0"
requires-python = ">=3.8"

[project.scripts]
run-orbslam = "run_orbslam:main"

[tool.setuptools]
py-modules = ["run_orbslam"]
